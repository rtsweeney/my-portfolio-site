---
phase: 03-homepage-and-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Coding/rysite/src/app/page.tsx
  - Coding/rysite/src/components/Navigation.tsx
  - Coding/rysite/src/app/globals.css
  - Coding/rysite/src/app/blog/page.tsx
  - Coding/rysite/next.config.mjs
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Visitor landing on / can see section cards for /concerts, /travel, /projects, and /resume"
    - "Hero subtitle names concerts, travel, projects, and resume — not 'projects, ideas, tools, and life updates'"
    - "Site navigation bar shows Concerts and Travel links (no Blog/Life Updates link)"
    - "Visiting /blog returns a permanent redirect to /concerts, not a live feed"
    - "Build completes without errors after all changes"
  artifacts:
    - path: "Coding/rysite/src/app/page.tsx"
      provides: "Homepage with 4-card grid: /concerts, /travel, /projects, /resume"
      contains: "card-accent-pink"
    - path: "Coding/rysite/src/components/Navigation.tsx"
      provides: "Nav links for /concerts and /travel; no /blog link"
      contains: "/concerts"
    - path: "Coding/rysite/src/app/globals.css"
      provides: "card-accent-orange CSS class"
      contains: "card-accent-orange"
    - path: "Coding/rysite/src/app/blog/page.tsx"
      provides: "permanentRedirect to /concerts"
      contains: "permanentRedirect"
    - path: "Coding/rysite/next.config.mjs"
      provides: "redirects() entry for /blog -> /concerts"
      contains: "redirects"
  key_links:
    - from: "Coding/rysite/src/app/page.tsx"
      to: "/concerts"
      via: "Link href"
      pattern: "href=\"/concerts\""
    - from: "Coding/rysite/src/app/page.tsx"
      to: "/travel"
      via: "Link href"
      pattern: "href=\"/travel\""
    - from: "Coding/rysite/src/components/Navigation.tsx"
      to: "/concerts"
      via: "nav-link"
      pattern: "href=\"/concerts\""
    - from: "Coding/rysite/src/app/blog/page.tsx"
      to: "/concerts"
      via: "permanentRedirect"
      pattern: "permanentRedirect"
---

<objective>
Update the homepage, navigation, and /blog route so the site accurately reflects what it is: concerts, travel, projects, and resume.

Purpose: Phases 1 and 2 built /concerts and /travel — now the entry points (homepage and nav) must point to them. The old /blog feed disappears, replaced by a permanent redirect.
Output: Updated page.tsx (4 real section cards + new hero subtitle), updated Navigation.tsx (/concerts and /travel links replacing /blog), card-accent-orange in globals.css, and /blog permanently redirecting to /concerts via both blog/page.tsx and next.config.mjs.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key patterns extracted from the codebase. Use these directly. -->

From src/app/globals.css (line 431-434) — existing card-accent classes:
```css
.card-accent-teal   { border-top: 3px solid var(--accent-secondary); }
.card-accent-purple { border-top: 3px solid var(--accent-primary); }
.card-accent-pink   { border-top: 3px solid var(--accent-warm); }
.card-accent-gold   { border-top: 3px solid var(--accent-gold); }
```
`--accent-orange: #e17055` already exists in :root (line 15).

From src/components/Navigation.tsx — nav-link pattern:
```tsx
<Link href="/projects" className={`nav-link ${isActive('/projects') ? 'active' : ''}`}>
  Projects
</Link>
```

From src/app/page.tsx — home card pattern:
```tsx
<Link href="/projects" style={{ textDecoration: 'none' }}>
  <div className="card card-accent-purple">
    <div className="home-card-icon">&#128736;</div>
    <h3 className="home-card-title">Projects</h3>
    <p className="home-card-desc">...</p>
  </div>
</Link>
```

From next.config.mjs — current shape (no redirects yet):
```js
const nextConfig = {
  images: {
    remotePatterns: [{ protocol: 'https', hostname: 'cdn.sanity.io' }],
  },
};
export default nextConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update homepage cards, hero subtitle, and add card-accent-orange CSS</name>
  <files>
    Coding/rysite/src/app/page.tsx
    Coding/rysite/src/app/globals.css
  </files>
  <action>
**globals.css** — add `card-accent-orange` on line 435 (directly after the existing four card-accent lines):
```css
.card-accent-orange { border-top: 3px solid var(--accent-orange); }
```

**page.tsx** — make three targeted changes:

1. Replace the hero subtitle paragraph (currently "A little corner of the internet for projects, ideas, tools, and life updates. Poke around...") with:
```tsx
<p className="hero-subtitle animate-in animate-delay-3">
  Concerts I&apos;ve been to. Places I&apos;ve traveled. Projects I&apos;ve built.
  My resume, too &mdash; but honestly the other stuff is more fun.
</p>
```

2. Replace the third card (currently /blog, card-accent-pink, "Life Updates") with a /concerts card:
```tsx
<Link href="/concerts" style={{ textDecoration: 'none' }}>
  <div className="card card-accent-pink">
    <div className="home-card-icon">&#127925;</div>
    <h3 className="home-card-title">Concerts</h3>
    <p className="home-card-desc">
      Shows I&apos;ve attended, rated and reviewed. From dive bars to arenas.
    </p>
  </div>
</Link>
```

3. Replace the fourth card (currently /calculators, card-accent-gold, "Calculators") with a /travel card using the new orange accent:
```tsx
<Link href="/travel" style={{ textDecoration: 'none' }}>
  <div className="card card-accent-orange">
    <div className="home-card-icon">&#127758;</div>
    <h3 className="home-card-title">Travel</h3>
    <p className="home-card-desc">
      Cities and countries I&apos;ve visited, mapped out with photos and notes.
    </p>
  </div>
</Link>
```

Keep the /projects (card-accent-purple) and /resume (card-accent-teal) cards exactly as-is. Keep hero CTAs ("See My Projects" + "View Resume") exactly as-is. Keep "What's Here" section title and subtitle exactly as-is. The home-grid will now have 4 cards: /projects, /resume, /concerts, /travel — matching the current grid layout.
  </action>
  <verify>
    <automated>cd /home/user/sweeney-town/Coding/rysite && npm run lint</automated>
  </verify>
  <done>page.tsx has hero subtitle naming concerts/travel/projects/resume; home-grid has cards for /concerts (card-accent-pink) and /travel (card-accent-orange); /blog and /calculators cards are gone; globals.css has card-accent-orange class; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update Navigation — add /concerts and /travel, remove /blog</name>
  <files>
    Coding/rysite/src/components/Navigation.tsx
  </files>
  <action>
In Navigation.tsx, replace the single `/blog` nav-link:
```tsx
<Link href="/blog" className={`nav-link ${isActive('/blog') ? 'active' : ''}`}>
  Life Updates
</Link>
```
with two new links inserted after the `/resume` link and before the Calculators dropdown:
```tsx
<Link href="/concerts" className={`nav-link ${isActive('/concerts') ? 'active' : ''}`}>
  Concerts
</Link>
<Link href="/travel" className={`nav-link ${isActive('/travel') ? 'active' : ''}`}>
  Travel
</Link>
```

Do not change any other nav links (Home, Projects, Resume), the Calculators dropdown, or the social icons. The final nav-links order will be: Home, Projects, Resume, Concerts, Travel, Calculators dropdown, social icons.
  </action>
  <verify>
    <automated>cd /home/user/sweeney-town/Coding/rysite && npm run lint</automated>
  </verify>
  <done>Navigation.tsx has /concerts and /travel links using the nav-link + isActive() pattern; /blog link is removed; lint passes.</done>
</task>

<task type="auto">
  <name>Task 3: Retire /blog — permanentRedirect in page.tsx + next.config.mjs redirects entry</name>
  <files>
    Coding/rysite/src/app/blog/page.tsx
    Coding/rysite/next.config.mjs
  </files>
  <action>
**blog/page.tsx** — replace the entire file contents with:
```tsx
import { permanentRedirect } from 'next/navigation';

export default function BlogPage() {
  permanentRedirect('/concerts');
}
```
This is a server component (no 'use client') that fires a 308 permanent redirect at render time on Cloudflare Pages edge. Delete all previous content (BLOG_QUERY, safeFetch, PortableText imports, BlogPost interface, TAG_CLASS_MAP, formatBlogDate, etc.).

**next.config.mjs** — add a `redirects()` async function as belt-and-suspenders for any infrastructure-level redirect (CDN, sitemap crawlers). Replace the current config with:
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'cdn.sanity.io',
      },
    ],
  },
  async redirects() {
    return [
      { source: '/blog', destination: '/concerts', permanent: true },
    ];
  },
};

export default nextConfig;
```
  </action>
  <verify>
    <automated>cd /home/user/sweeney-town/Coding/rysite && npm run build</automated>
  </verify>
  <done>blog/page.tsx contains only permanentRedirect('/concerts') — no Sanity query, no PortableText; next.config.mjs has redirects() returning [{ source: '/blog', destination: '/concerts', permanent: true }]; build completes without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Homepage updated: hero subtitle names concerts/travel/projects/resume; home-grid has 4 cards (/projects, /resume, /concerts, /travel) with correct accent colors (purple, teal, pink, orange)
    - Navigation updated: Concerts and Travel links visible; Life Updates/Blog link gone
    - /blog permanently redirects to /concerts (308)
    - Build passes
  </what-built>
  <how-to-verify>
    1. Run `cd /home/user/sweeney-town/Coding/rysite && npm run dev` and open http://localhost:3000
    2. Homepage (/): Confirm hero subtitle mentions concerts, travel, projects, and resume. Confirm 4 section cards appear — Projects (purple top), Resume (teal top), Concerts (pink top, guitar icon), Travel (orange top, globe icon).
    3. Click the Concerts card — should navigate to /concerts.
    4. Click the Travel card — should navigate to /travel.
    5. Navigation bar: Confirm "Concerts" and "Travel" links are present. Confirm "Life Updates" is gone.
    6. Visit http://localhost:3000/blog — should redirect to /concerts (check browser address bar).
    7. Confirm the page feels personal and site-specific, not generic portfolio boilerplate.
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any visual issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Wave 1 gate (after Tasks 1-2): `npm run lint` passes.
Wave 2 gate (after Task 3): `npm run build` passes — zero TypeScript errors, zero build failures.
Phase gate: Human visual review confirms homepage and nav match the design intent (checkpoint:human-verify).
</verification>

<success_criteria>
- HOME-01: Homepage hero subtitle names the four real sections (concerts, travel, projects, resume)
- HOME-02: Four section cards on homepage each link to /concerts, /travel, /projects, /resume — all reachable in one click
- HOME-03: Homepage feels personal and well-crafted (confirmed by human review checkpoint)
- NAV-01: Navigation bar includes visible /concerts link
- NAV-02: Navigation bar includes visible /travel link
- NAV-03: /blog route no longer serves a live feed — permanentRedirect('/concerts') fires for all visitors
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-and-navigation/03-01-SUMMARY.md` using the summary template at @/root/.claude/get-shit-done/templates/summary.md.
</output>
