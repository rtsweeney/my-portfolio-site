---
phase: 02-travel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Coding/rysite/src/sanity/schemaTypes/travel.ts
  - Coding/rysite/src/sanity/schemaTypes/index.ts
autonomous: true
requirements: [TRVL-05, TRVL-06]

must_haves:
  truths:
    - "Ryan can open /studio and see Travel as a document type"
    - "The Travel form shows city, country, coordinates (geopoint), date, rating, description, and photos fields"
    - "Ryan can save a travel entry with coordinates and it persists in Sanity"
  artifacts:
    - path: "Coding/rysite/src/sanity/schemaTypes/travel.ts"
      provides: "Travel document type definition"
      contains: "defineType({ name: 'travel'"
    - path: "Coding/rysite/src/sanity/schemaTypes/index.ts"
      provides: "Schema registration"
      contains: "import { travel }"
  key_links:
    - from: "Coding/rysite/src/sanity/schemaTypes/travel.ts"
      to: "Coding/rysite/src/sanity/schemaTypes/index.ts"
      via: "named export + import + types array registration"
      pattern: "types: \\[.*travel.*\\]"
---

<objective>
Create the Sanity travel schema and register it in Studio so Ryan can manage travel entries with all required fields.

Purpose: Foundation for the /travel map page — the schema must exist before GROQ queries and the page component can be built.
Output: travel.ts schema file and updated index.ts registration.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-travel/02-RESEARCH.md
</context>

<interfaces>
<!-- Existing patterns the executor should mirror exactly -->

From Coding/rysite/src/sanity/schemaTypes/concert.ts (mirror this structure):
```typescript
import { defineField, defineType } from 'sanity';

export const concert = defineType({
  name: 'concert',
  title: 'Concert',
  type: 'document',
  fields: [
    defineField({ name: 'rating', type: 'number', options: { list: [1, 2, 3, 4, 5] } }),
    defineField({
      name: 'photos', type: 'array',
      of: [{ type: 'image', options: { hotspot: true },
             fields: [defineField({ name: 'alt', type: 'string' })] }],
    }),
    // ... other string/date fields
  ],
  preview: { select: { title: 'city', subtitle: 'country', media: 'photos.0' } }
});
```

From Coding/rysite/src/sanity/schemaTypes/index.ts (current state):
```typescript
import { type SchemaTypeDefinition } from 'sanity'
import { resume } from './resume'
import { photo } from './photo'
import { project } from './project'
import { blogPost } from './blogPost'
import { concert } from './concert'

export const schema: { types: SchemaTypeDefinition[] } = {
  types: [resume, photo, project, blogPost, concert],
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create travel.ts Sanity schema</name>
  <files>Coding/rysite/src/sanity/schemaTypes/travel.ts</files>
  <action>
Create a new file at `Coding/rysite/src/sanity/schemaTypes/travel.ts`. Mirror the concert.ts pattern exactly. The travel document type must include all 7 required fields:

```typescript
import { defineField, defineType } from 'sanity';

export const travel = defineType({
  name: 'travel',
  title: 'Travel',
  type: 'document',
  fields: [
    defineField({ name: 'city', title: 'City', type: 'string', validation: (r) => r.required() }),
    defineField({ name: 'country', title: 'Country', type: 'string', validation: (r) => r.required() }),
    defineField({ name: 'coordinates', title: 'Coordinates', type: 'geopoint', validation: (r) => r.required() }),
    defineField({ name: 'date', title: 'Date Visited', type: 'date', validation: (r) => r.required() }),
    defineField({
      name: 'rating', title: 'Rating (1–5)', type: 'number',
      validation: (r) => r.required().min(1).max(5).integer(),
      options: { list: [1, 2, 3, 4, 5] },
    }),
    defineField({ name: 'description', title: 'Description', type: 'text', rows: 4 }),
    defineField({
      name: 'photos', title: 'Photos', type: 'array',
      of: [{
        type: 'image', options: { hotspot: true },
        fields: [defineField({ name: 'alt', title: 'Alt Text', type: 'string' })],
      }],
    }),
  ],
  preview: {
    select: { title: 'city', subtitle: 'country', media: 'photos.0' },
    prepare({ title, subtitle, media }) {
      return { title, subtitle, media };
    },
  },
});
```

Key notes:
- Use Sanity's built-in `geopoint` type for coordinates — renders a map picker in Studio and stores lat/lng/alt natively
- Rating uses `options: { list: [1, 2, 3, 4, 5] }` with number validation — same pattern as concert.ts
- Photos are inline image objects with hotspot (not references to photo document type) — same pattern as concert.ts
- description is `text` with rows: 4 (not richtext/portable text — keeps it simple)
  </action>
  <verify>
    <automated>test -f Coding/rysite/src/sanity/schemaTypes/travel.ts && grep -q "geopoint" Coding/rysite/src/sanity/schemaTypes/travel.ts && grep -q "coordinates" Coding/rysite/src/sanity/schemaTypes/travel.ts && echo "travel.ts OK"</automated>
  </verify>
  <done>travel.ts exists with all 7 fields including geopoint coordinates; file follows concert.ts pattern</done>
</task>

<task type="auto">
  <name>Task 2: Register travel schema in index.ts</name>
  <files>Coding/rysite/src/sanity/schemaTypes/index.ts</files>
  <action>
Update `Coding/rysite/src/sanity/schemaTypes/index.ts` to import and register the travel schema. Add `import { travel } from './travel'` alongside the other imports, and add `travel` to the `types` array:

```typescript
import { type SchemaTypeDefinition } from 'sanity'
import { resume } from './resume'
import { photo } from './photo'
import { project } from './project'
import { blogPost } from './blogPost'
import { concert } from './concert'
import { travel } from './travel'

export const schema: { types: SchemaTypeDefinition[] } = {
  types: [resume, photo, project, blogPost, concert, travel],
}
```

Do not reorder existing entries — append travel import at the bottom of the imports block and add travel at the end of the types array.
  </action>
  <verify>
    <automated>grep -q "import { travel } from './travel'" Coding/rysite/src/sanity/schemaTypes/index.ts && grep -q "travel," Coding/rysite/src/sanity/schemaTypes/index.ts && echo "index.ts registration OK"</automated>
  </verify>
  <done>index.ts imports travel and includes it in the types array; Sanity Studio will discover the Travel document type</done>
</task>

</tasks>

<verification>
After both tasks:
- `Coding/rysite/src/sanity/schemaTypes/travel.ts` exists with all 7 required fields (city, country, coordinates/geopoint, date, rating, description, photos)
- `Coding/rysite/src/sanity/schemaTypes/index.ts` imports and registers travel
- Schema follows existing concert.ts conventions exactly
</verification>

<success_criteria>
- travel.ts created with: city (string), country (string), coordinates (geopoint), date (date), rating (number 1-5 with list), description (text), photos (inline image array with hotspot + alt)
- index.ts updated: travel imported and added to types array
- No modifications to any other schema files
</success_criteria>

<output>
After completion, create `.planning/phases/02-travel/02-01-SUMMARY.md`
</output>
